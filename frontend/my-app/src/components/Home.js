export default function Home() {

    //const navigate = useNavigate();

    return <h1>Home</h1>
    /*return(
        <div>
            <p>Home</p>
            <button onClick={() => {
                navigate("/users")
            }}> Go to users! </button>
        </div>
    )*/
}